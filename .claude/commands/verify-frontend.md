Build the frontend for the AI Hiring Platform and report the result.

```
cd ai_hiring_platform/frontend
npm run build
```

This runs `tsc -b && vite build`. Report success or the first type/build error with its file and line. Type errors must be fixed, not suppressed. Optionally also run `npm run lint` (oxlint).
