Build the frontend production bundle for the AI Hiring Platform.

```
cd ai_hiring_platform/frontend
npm run build
```

Runs `tsc -b && vite build`. Report success (module count + time) or the first error with file:line. Type errors must be fixed at the source, never suppressed.
